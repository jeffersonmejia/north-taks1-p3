@model RegisterViewModel
@{
    ViewData["Title"] = "Register";
}
<section class="auth-wrap">
    <form class="glass-panel auth-card register-card fade-in" asp-action="Register" method="post">
        <div class="auth-head">
            <h1>Create account</h1>
            <p>Use your customer account to browse products and place orders.</p>
        </div>
        <div asp-validation-summary="ModelOnly" class="validation"></div>
        <div class="auth-grid">
            <div class="field">
                <label asp-for="FullName"></label>
                <input asp-for="FullName" />
                <span asp-validation-for="FullName" class="validation"></span>
            </div>
            <div class="field">
                <label asp-for="Email"></label>
                <input asp-for="Email" autocomplete="email" />
                <span asp-validation-for="Email" class="validation"></span>
            </div>
            <div class="field">
                <label asp-for="Password"></label>
                <input asp-for="Password" autocomplete="new-password" />
                <span asp-validation-for="Password" class="validation"></span>
            </div>
            <div class="field">
                <label asp-for="ConfirmPassword"></label>
                <input asp-for="ConfirmPassword" autocomplete="new-password" />
                <span asp-validation-for="ConfirmPassword" class="validation"></span>
            </div>
        </div>
        <div class="auth-actions">
            <button class="btn btn-primary" type="submit">Register</button>
            <a class="btn btn-ghost" asp-action="Login">Login</a>
        </div>
    </form>
</section>
